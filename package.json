{
  "name": "@papaya/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since",
    "dev": "lerna run start",
    "dev:app": "lerna run start --scope=@papaya/breathing-app",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
